for variable in datos:
    print()

for variable, valor in datos.items():
    print()

for variable in range(1,10):
    print()
-----------------------------------------------

### Ejercicio 1: Iterando sobre una lista 
1. Crea una lista con cinco nombres de personas.
2. Usa un bucle `for` para imprimir cada nombre de la lista en una línea separada.


### Ejercicio 2: Iterando sobre un diccionario con `items()`
1. Crea un diccionario que contenga tres elementos clave-valor, donde las claves sean nombres de frutas y los valores sean sus colores.
2. Usa un bucle `for` para iterar sobre los elementos del diccionario e imprime tanto la fruta como su color en formato "Fruta: color".


### Ejercicio 3: Iterando sobre un rango de números
1. Escribe un programa que use un bucle `for` para imprimir los números del 1 al 9 (inclusive), usando `range()`.
2. En cada iteración, imprime el número junto con el mensaje "Este es el número X", donde `X` es el número actual.